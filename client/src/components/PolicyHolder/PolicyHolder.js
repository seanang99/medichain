import React from 'react';
import PropTypes from 'prop-types';


const PolicyHolder = () => (
  <div>
    PolicyHolder Component
  </div>
);

PolicyHolder.propTypes = {};

PolicyHolder.defaultProps = {};

export default PolicyHolder;
